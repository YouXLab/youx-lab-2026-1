import math
angulo= float(input('o angulo vale: '))
seno= math.sin(math.radians(angulo))
cosseno=math.cos(math.radians(angulo))
tangente=math.tan(math.radians(angulo))
print(f'o seno vale {seno:.1f} - o cosseno vale {cosseno:.1f} - e o tangente vale {tangente:.1f}')